package lt.techin.mantaspovilas.numbers;

import lt.itakademija.exam.NumberFilter;

public class Filter implements NumberFilter {
    @Override
    public boolean accept(int i) {
        return true;
    }
}
